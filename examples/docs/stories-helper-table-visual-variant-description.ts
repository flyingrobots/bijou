import type { TableVisualVariantSpec } from './stories-helper-table-visual-variant-spec.js';

export function tableVisualVariantDescription(spec: TableVisualVariantSpec): string {
  return `Demonstrates table() variant ${spec.variant}.`;
}
