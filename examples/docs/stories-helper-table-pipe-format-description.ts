import type { TablePipeFormatSpec } from './stories-helper-table-pipe-format-spec.js';

export function tablePipeFormatDescription(spec: TablePipeFormatSpec): string {
  return `Demonstrates pipeFormat ${spec.pipeFormat}.`;
}
